import { TopNav } from '~/components/Layout/TopNav';

export const Header = () => (
  <header>
    <h2 className="text-shadow py-4 text-center text-cyan-700 text-3xl md:text-5xl">
      Pack your bags, we're getting married in Jamaica!
    </h2>

    <TopNav />
  </header>
);
